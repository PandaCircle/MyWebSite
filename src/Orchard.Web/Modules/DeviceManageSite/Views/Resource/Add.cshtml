@{

    var resType = (string)Model.resType;
}
<legend>添加资源（@resType）:</legend>

@using (Html.BeginFormAntiForgeryPost()) {
    @Html.ValidationSummary()
    @Html.Hidden("resType",resType)
    @Display(Model)
}

